astroCopy code
---
// src/component/PostCard.astro
const { post } = Astro.props;
---

<div class="post-card">
  <h2>{post.title}</h2>
  <p>{post.body}</p>
  <a href={`/post/${post.id}`}>Ver detalles</a>
</div>

